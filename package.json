{
  "name": "daily-wins",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx convex dev",
    "build": "npx convex deploy"
  },
  "dependencies": {
    "convex": "^1.35.1"
  }
}
